<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.digging.spring.ex.mybatis.repository.ReviewRepository">
 	<select id="selectReview"
 	resultType="com.digging.spring.ex.mybatis.domain.Review">
 		SELECT
 			`id`
 			, `storeId`
 			, `menu`
 			, `userName`
 			, `point`
 			, `review`
 			, `createdAt`
 			, `updatedAt
 		FROM
 			`used_goods`
 		WHERE
 			`id` = #{id}
 	</select>
 
 </mapper>